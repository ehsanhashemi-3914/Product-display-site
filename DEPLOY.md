# راهنمای انتشار سایت

هدف: یک لینک که همیشه بالا باشد، و هر کسی با هر گوشی‌ای بازش کند محصولات شما را ببیند.

کل کار سه بخش است: **گیت‌هاب → ورسل → دیتابیس**. حدود ۱۵ دقیقه.

---

## قدم ۰ — دو مقدار که لازم دارید

قبل از شروع این دو را یادداشت کنید:

**۱. رمز پنل مدیریت** — هر چه دوست دارید. مثلاً `Ehsan@1404Mohr`

**۲. یک کلید امنیتی تصادفی** — این دستور را در ترمینال بزنید و خروجی را کپی کنید:

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

> این‌ها را در هیچ فایلی داخل پروژه ننویسید. فقط در پنل ورسل وارد می‌شوند.

---

## قدم ۱ — گذاشتن کد روی گیت‌هاب

اگر ریپو ندارید، در گیت‌هاب یک ریپوی **خصوصی (Private)** بسازید، بعد در ترمینال پروژه:

```bash
git init
```

```bash
git add . && git commit -m "سایت مهر نماز احسان"
```

```bash
git remote add origin https://github.com/USERNAME/REPO.git && git branch -M main && git push -u origin main
```

> فایل `.env.local` خودکار نادیده گرفته می‌شود و روی گیت‌هاب نمی‌رود. رمزهایتان آنجا لو نمی‌رود.

---

## قدم ۲ — ساختن پروژه در ورسل

1. به [vercel.com](https://vercel.com) بروید و با همان اکانت گیت‌هاب ثبت‌نام کنید.
2. **Add New… → Project** را بزنید.
3. ریپوی پروژه را انتخاب و **Import** کنید.
4. در همان صفحه، بخش **Environment Variables** را باز کنید و این دو را وارد کنید
   (برای مورد دوم دکمهٔ **Add More** را بزنید):

| Key | Value |
| --- | --- |
| `ADMIN_PASSWORD` | رمزی که در قدم ۰ انتخاب کردید |
| `AUTH_SECRET` | همان رشتهٔ تصادفی قدم ۰ |

5. **Deploy** را بزنید.

> تب **Storage** در این صفحه وجود ندارد؛ فقط بعد از ساخته شدن پروژه ظاهر می‌شود.
> برای همین اول Deploy می‌کنیم، بعد دیتابیس را وصل می‌کنیم.

---

## قدم ۳ — ساختن و وصل کردن دیتابیس

بعد از پایان دیپلوی وارد داشبورد پروژه می‌شوید. در نوار بالا:

`Overview` · `Deployments` · `Analytics` · **`Storage`** · `Settings`

1. تب **Storage** را باز کنید.
2. **Create Database → Postgres** (گزینهٔ Neon) را انتخاب کنید.
3. نام دلخواه بدهید و بسازید.
4. حتماً **Connect to Project** را بزنید و این پروژه را انتخاب کنید.

ورسل خودش آدرس اتصال را به متغیرهای پروژه اضافه می‌کند؛ لازم نیست چیزی کپی کنید.
جدول‌ها را هم لازم نیست بسازید — سایت در اولین اجرا خودش می‌سازد و محصولات نمونه را
می‌گذارد.

---

## قدم ۴ — یک دیپلوی دوباره

تب **Deployments** → روی آخرین دیپلوی سه‌نقطه (`···`) → **Redeploy**.

این کار لازم است تا برنامه آدرس دیتابیس تازه‌وصل‌شده را ببیند.

> بین قدم ۲ و اینجا، سایت بالا هست ولی محصولی نشان نمی‌دهد. طبیعی است و بعد از
> Redeploy درست می‌شود.

بعد از یکی دو دقیقه لینکی مثل این می‌گیرید:

```
https://your-project.vercel.app
```

همین لینک را به مشتری بدهید.

پنل مدیریت شما: `https://your-project.vercel.app/admin`

---

## بعد از انتشار

- محصولات را **یک‌بار** از پنل وارد کنید. روی سرور ذخیره می‌شوند و برای همیشه می‌مانند.
- هر تغییری بدهید، بلافاصله برای همه اعمال می‌شود. نیازی به دیپلوی دوباره نیست.
- تصاویر را مستقیم از گوشی یا کامپیوتر آپلود کنید؛ خودکار کوچک و بهینه می‌شوند.
- برای عوض کردن رمز: مقدار `ADMIN_PASSWORD` را در ورسل تغییر دهید و یک‌بار Redeploy کنید.
  با این کار همهٔ ورودهای قبلی روی همهٔ دستگاه‌ها هم باطل می‌شوند.

---

## اگر ورسل از ایران باز نشد

ثبت‌نام و پنل ورسل ممکن است بدون تحریم‌شکن کار نکند. در آن صورت **لیارا** جایگزین کاملاً معادل است و کد هیچ تغییری لازم ندارد:

1. در [liara.ir](https://liara.ir) ثبت‌نام کنید.
2. یک **دیتابیس PostgreSQL** بسازید و رشتهٔ اتصالش را بردارید.
3. یک **برنامهٔ Node.js** بسازید.
4. در تنظیمات برنامه این سه متغیر را وارد کنید:
   - `DATABASE_URL` → رشتهٔ اتصال دیتابیس
   - `ADMIN_PASSWORD` → رمز پنل
   - `AUTH_SECRET` → همان رشتهٔ تصادفی
5. با `liara deploy` منتشر کنید.

همه‌چیز دقیقاً مثل ورسل کار می‌کند؛ فقط جای میزبان عوض می‌شود.

---

## اجرای محلی روی کامپیوتر خودتان

برای کار کردن روی سایت بدون اینترنت، در **دو ترمینال جدا**:

```bash
npm run db:local
```

```bash
npm run dev
```

بعد <http://localhost:3200> را باز کنید. تنظیمات محلی از قبل در `.env.local` آماده است.
